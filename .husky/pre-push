#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Full quality gates on every push: lint, format, typecheck, tests, coverage, audit, build.
echo "Running pre-push quality gates (full suite)..."
bash "$(dirname -- "$0")/../scripts/pre-push-gates.sh"
