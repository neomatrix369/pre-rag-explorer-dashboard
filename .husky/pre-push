#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Mirrors CI fast gates: lint, format, typecheck, unit tests (not coverage/audit/build).
echo "Running pre-push quality gates (npm run test:all)..."
npm run test:all
